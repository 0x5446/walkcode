# Deep Review 综合结论 — v0.14.15 频道环境上下文 + 无根绑定自愈 + SDK 缓冲上限

**VERDICT**: NEEDS_FIX → round 2 修复后 **SAFE（带已登记跟进项，无 Critical）**
**轮次**：2 / 3
**类型**：code

> 范围：PR #82 — 环境上下文注入（claude system prompt / codex 首 turn）+ Lark TUI 无根绑定自愈 + claude SDK max_buffer_size 64MiB
> Review engine：codex codex-cli 0.144.5（host: claude; engine_source: auto）
> Cursor：disabled（not logged in）
> 维度：8 个 codex 并行（全部成功，全部 NEEDS_FIX → 处置后收敛）
> Phase 2 验证：高共识项按 HIGH_CONF_SKIP_VERIFY 直接修复；其余以代码/线上证据处置（见"已驳回"）
> Repo: /Users/alpha/workspace/walkcode
> HeadSHA: 1286031（round 1 基线；round 2 修复 5df5ed5）
> RunDir: /var/folders/00/s7tt4dgj53v123y8671yb3b00000gn/T/deep-review-walkcode-1286031-1784990278.Rb0h
> 规模：+472/-3 行 / 5 文件（round 1 基线）

## 已修复（round 2，commit 5df5ed5）

### 1. [Warning] heal 失败冻结一次性刷新标记，本进程永不重试
- 来源：errors + data + correctness + observability + design + tests（最高共识，conf 0.99-1.0）
- 修复：heal 三态返回（""/healed/retry）；有 retry 时不置完成标记，维护 tick 持续重试；失败走 `_log_degrade`（带 session_id/chat_id/agent）。

### 2. [Warning] 原地改 binding key 不更新注册表索引，新话题回复解析不到会话
- 来源：correctness（conf 1.0）
- 修复：`replace()` 重建 binding 并走 `update_channel_binding` 重注册；同时清 `health_message_id`/`last_message_id` 让状态卡在新话题重建（顺带处置 data#2 的旧卡片部分）。

### 3. [Warning] 群聊场景凭据/二维码外发风险（security#1）
- 修复：模板增加第 5 条——聊天可能有他人时禁止外发凭据/二维码/带 token 链接。

### 4. [Warning] SDK 版本无下限，旧版逐 chunk 重解析 × 64MiB 放大（security#2）
- 修复：upgrade.sh 钉 `claude-agent-sdk>=0.2.124`（本机实装 0.2.128）。

### 5. [Warning] heal 发送期间会话竞态 stop 后仍回填（concurrency#1 部分）
- 修复：发送成功后复核 `session.status`，已 stop 则放弃回填返回 retry。

### 6. [Warning] 测试缺口（tests#1/#2）
- 修复：外层刷新"首败次成"重试测试、注册表换键断言、codex `_build_transports` 接线测试。

## ❌ 已驳回

- 无（round 1 全部发现均成立，按修复/登记处置）。

## 🟡 登记跟进（均非 Critical；多为既有行为或显式 opt-in 路径）

- **G1（correctness#3/design#1）**：`WALKCODE_CLAUDE_SPAWN_MODE=daemon`（`claude --bg`）出生的会话无环境说明注入面。已写入文档为已知缺口；本机 5 实例均为 headless spawn。
- **G2（concurrency#1 残余）**：heal 的发送对外层 15s 超时取消不免疫，极端下可能重复发根 notice（绑定最终仍会被下一轮 heal 补齐；无幂等键管道）。
- **G3（concurrency#2）**：codex `_env_context_pending/_delivered` 集合无 per-thread 锁；编排层基本串行，最坏后果是历史里前置块重复一次（装饰性）。
- **G4（data#2 残余）**：故障期间已按旧空根键入列的 outbox 项不迁移，恢复后仍发主会话（一次性存量）。
- **G5（data#3）**：根消息发出与 save_state 之间崩溃会在下次启动重复建根（孤立 notice；无飞书幂等键管道）。
- **G6（observability#1）**：codex `events()` 超时与正常收口不可区分（既有行为，非本 PR 引入）。
- **G7（observability#2）**：SDK 缺字段时环境说明/缓冲扩容静默降级，建议 log-once + doctor 暴露能力状态。

## 维度元信息

| 来源 | VERDICT | issues | exit |
|---|---|---|---|
| dim-correctness | NEEDS_FIX | 3 | 0 |
| dim-errors | NEEDS_FIX | 1 | 0 |
| dim-security | NEEDS_FIX | 2 | 0 |
| dim-concurrency | NEEDS_FIX | 2 | 0 |
| dim-data | NEEDS_FIX | 3 | 0 |
| dim-observability | NEEDS_FIX | 3 | 0 |
| dim-design | NEEDS_FIX | 2 | 0 |
| dim-tests | NEEDS_FIX | 2 | 0 |

## 验证

- 全量测试：970 passed, 24 subtests passed（round 2 修复后）
- 原始产物：RunDir 下 dim-*.md
