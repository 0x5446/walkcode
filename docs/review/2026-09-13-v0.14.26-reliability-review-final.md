# Deep Review 综合结论

VERDICT: SAFE

**轮次**：2 / 2；mixed；增量审查上一轮修复。

> Repo: /Users/alpha/workspace/walkcode-reliability-v0.14.26
> HeadSHA: 2ed8ca46594efc2ccee61f24dbeb5997db5fbae2
> RunDir: /var/folders/00/s7tt4dgj53v123y8671yb3b00000gn/T/deep-review-walkcode-reliability-v0.14.26-2ed8ca4-1789271251.1ACa
> WorktreeDigest: be1749384baafc2a0826b24a3017c5087a02798198aeb9c2c922f6bf13810379
> Review engine: Claude 2.1.270；claude-fable-5 high，fallback claude-opus-5；host Codex。
> Cursor: disabled（未登录）。correctness / goalfit / security / concurrency / data 全部 exit=0。
> Phase 2：1 条 FALSE_POSITIVE。无存活 Critical/Warning。

## 需求点交付核对与各维度结论

### goalfit

范围确认：读了 context.md、target.diff 全文，并核对了 `src/walkcode/channel_native_runtime.py` 的 `serve_lark_ws`（2173–2230）、`_process_lark_inbox_item`（2237–2264）、`_report_lark_inbound_failure`（2266–2293）、`_best_effort_flush_outbox`（2654–2662）、主路径 `process_lark_event` 的授权门（1937/1966–1968），以及 `serve_lark_ws` 全部调用点（含 `retry_delay` 残留检索）。

需求点核对表：

| R | 状态 | 位置与理由 |
|---|---|---|
| R1 | 已兑现 | `serve_lark_ws` 2220–2225：任何异常 `_log_degrade` 后 `raise` 退出消费循环；notice 保存失败时异常在 `claimed.rename(.failed)` 之前逃逸（2259–2260），`.processing` 保留；interrupted 分支 2250–2251 只发不确定通知，永不重投 agent。测试 `test_notice_serialization_failure_stops_consumer_and_retains_claim` 验证退出+保留+零 agent 提交。 |
| R2 | 已兑现 | 2284–2293：先 `outbox.enqueue` + `save_state()`，再 `_best_effort_flush_outbox()`（复用既有 5s `asyncio.wait_for`，2654–2662）；超时被吞、已保存的 notice 留在 outbox 等周期重试。测试 `test_notice_flush_timeout_retains_saved_outbox_and_finishes_inbox` 断言 pending=1 且含通知文本。 |
| R3 | 已兑现 | 2277 `if inbound.callback is None and not self._lark_sender_allowed(...)` 与主路径 1966–1968 完全同构；chat 门 2275 保留（含 `is_callback` 参数）；通知只 enqueue 固定文本，不触碰 callback action。正反两个测试（callback 出通知、未授权普通消息零 API 调用）都在。 |
| R4 | 已兑现 | AGENTS.md 新增 Durable delivery 条目（指回 ARCHITECTURE.md）；ARCHITECTURE.md 237 段改为 "Recovery failures stop the consumer" + 5 秒 flush 描述，与新代码行为一致；明确 "not exactly-once"。无新 ADR、无新依赖。 |
| R5 | 已兑现 | 四个新测试逐一对应：序列化失败退出、flush 超时保留、callback 通知放行、未授权普通消息不发通知（tests/test_channel_native_lark.py:674–157 区间四个用例）。 |
| R6 | 已兑现 | docs/lark-profile-deploy.md 新增 2026-09-13 实机记录：app id `cli_aa8ff6ae7e781e18` ↔ `personal-claude.env` 映射、`99991403` 配额失败、outbox.dead 去向、"贴表情只表示收到"语义保留、恢复条件。与 context 授权范围一致（迁移结果后续另记）。 |

证伪方向也查过：`retry_delay` 参数删除后无残留调用点（仅 `serve_telegram_polling` 自有同名参数，未动，属另一传输通道的存量行为，非本次范围）；`_report_lark_inbound_failure` 内 `ledger.complete` 在 `save_state` 之前但失败时状态不落盘、重启后 idempotency_key 防重复通知，重入安全；文档三处与代码行为逐句对得上。无范围蔓延、无重复造轮子（flush 与两个授权门均复用既有组件）、删除重试参数属最简方向。

零 ISSUE。

VERDICT: SAFE
SUMMARY: 六个需求点全部能在代码或文档里指到具体兑现位置，复用了既有的限时刷新和授权门，未发现目标偏离或漏交付。


### correctness

范围确认：审了 target.diff 全部五个文件的改动，并读了 `channel_native_runtime.py` 的 `serve_lark_ws` / `_process_lark_inbox_item` / `_report_lark_inbound_failure` / `_best_effort_flush_outbox` / `_lark_chat_allowed` / `_lark_sender_allowed` 及主路径 `process_lark_event` 的门禁顺序，另核了 `channel_native/__init__.py` 里 `Outbox.enqueue` 幂等键去重（pending/sent/dead 三桶都查）、`InboundLedger` 生命周期和 `parse_event` 的异常面。

逐条核对（correctness 相关）：
- R1：`serve_lark_ws` 2220-2225 任何逃逸异常记录后 re-raise 退出；`_report_lark_inbound_failure` 抛错时 2260 的 `.failed` 归档不执行，`.processing` 保留；重放路径 2250-2251 只发通知、不重投 agent。已兑现。
- R2：2292 先 `save_state` 落盘通知，再 2293 走 `_best_effort_flush_outbox`（5 秒 `wait_for`、异常吞掉），超时后 pending 保留，由 `_flush_outbox_forever` 周期重试。已兑现。
- R3：2277 `inbound.callback is None` 才查发送者白名单，与主路径 1966-1967 的门禁结构一致；通知是固定文本，不含回调动作；聊天白名单 2275 保留。已兑现。
- R5：四个新测试与四个断言点一一对应，逐个推演了执行路径（含 `save_state` 全局 patch 不影响先抛的 `RuntimeError`），成立。

主动证伪过的攻击面，均未击穿：
- 毒载荷崩溃循环：合法 JSON 但形状畸形（event 为 null/字符串/列表、payload 非 dict）在 `parse_event` 只会产生 AttributeError/TypeError/ValueError，全部落在 2270 的捕获元组内，归档 `.failed` 隔离，不会让消费者在同一文件上无限重启。
- 重复通知：`.failed` 归档失败后重启重放会再次 enqueue，但幂等键 `lark-inbound-failed:{event_id}` 在 pending/sent/dead 三桶去重，不双发（compact 清掉 sent 后理论可重发，已被"非 exactly-once"边界文档覆盖）。
- 成功后 `unlink` 失败：重启后误发一次"结果未能确认"通知，通知文案本身要求用户先看话题结果，属声明过的语义。
- 回调空 chat_id 走 `_lark_chat_allowed` 豁免后通知绑定 chat 为空：与主路径既有豁免一致，回调必有 message_id 可作回复锚点，非本次引入。
- `flush_once` 换成 `_best_effort_flush_outbox` 后吞掉 `ChannelConfigError`：通知已落盘，维护任务会重试，行为反而更稳，符合 R2。
- 删除 `retry_delay` 后无残留调用方（2511 的 `retry_delay` 属 `serve_telegram_polling`，拉取式轮询无 durable claim，语义不同，不受影响）。

无 ISSUE。

VERDICT: SAFE
SUMMARY: 增量修复的异常传播顺序、门禁一致性、幂等去重和落盘先后都经得起推演，正确性维度未发现实质问题。


### security

**范围确认**：审查了 target.diff 全部四个变更文件（`AGENTS.md`、`ARCHITECTURE.md`、`docs/lark-profile-deploy.md`、`src/walkcode/channel_native_runtime.py`、`tests/test_channel_native_lark.py`），并读取了仓库内 `_report_lark_inbound_failure`、`_process_lark_inbox_item`、`_lark_chat_allowed`/`_lark_sender_allowed`、`_best_effort_flush_outbox`（channel_native_runtime.py:1832-1853, 2237-2293, 2654-2662）及 `LarkChannelAdapter.parse_event` 与全部内容解析辅助函数（channel_native/__init__.py:5407-5800）。维度：security。

安全面核对结论：

- **入站 payload 毒消息循环**：`parse_event` 全程用 `.get` 取值，`body["content"]`、`segment["image_key"]` 两处裸索引均有前置类型/真值守卫；恶意 payload 能触发的异常面（AttributeError/TypeError/ValueError，json 错误是 ValueError 子类）被 `_report_lark_inbound_failure` 的 quarantine 元组覆盖，归档 `.failed` 不进崩溃循环。磁盘类失败导致退出重启是文档化的设计决策（ARCHITECTURE.md），且 context 明确不加新 supervisor，不报。
- **通知内容注入/泄漏**：失败通知为固定中文文案，不含攻击者可控内容；异常文本只进 `last_lark_event_error` 与运维日志，无聊天面外显路径。
- **未授权普通消息**：测试确认既无通知也不写 ledger，不给未授权发送者留探测回声。符合主路径 gate 语义（主路径 1966 行对 callback 同样跳过 sender 白名单，R3 的对齐是正确方向）。
- **文档**：`cli_aa8ff6ae7e781e18` 是 app id（公开标识符），非 secret，无泄漏。

仅一条纵深防御建议：

### ISSUE_1
- Plain: 卡片回调若不带会话标识，失败通知路径没有任何身份关卡，还会白白留下一条永远发不出去的待发消息。
- Severity: Suggestion
- Category: Security
- File: src/walkcode/channel_native_runtime.py
- StartLine: 2275
- EndLine: 2291
- Symbol: _report_lark_inbound_failure
- Confidence: 0.6
- Evidence: `_lark_chat_allowed`（1836-1839 行）对 `is_callback and not chat_id` 直接放行，注释写明"callback token 才是真正的门"；但 `_report_lark_inbound_failure` 不校验 token，随后执行 `ledger.complete` 并 `outbox.enqueue` 到一个 `chat_id` 为空的 binding。
- Problem: 空 chat_id 的回调 payload 在此路径上零关卡通过（chat 守卫空转、sender 守卫按 R3 跳过、token 守卫缺失），产生授权前副作用：写 inbound ledger、往 outbox 塞一条因缺 chat_id 而永远投递失败的通知（最终落 dead，靠 compact 清理）。可利用性低——需先让 `process_lark_event` 抛异常——但它违背了 1837 行注释声称的"token 是真正的门"这一不变式。
- Fix: `_report_lark_inbound_failure` 在 chat 守卫前加一行提前返回：`if inbound.callback is not None and not inbound.chat_id: return`——没有 chat_id 的通知本来就无处投递，顺带关掉这条无门路径。

VERDICT: SAFE
SUMMARY: 授权收敛、通知文案固定、毒消息已隔离，本维度无实质风险；仅建议对无会话标识的回调提前短路，消除一条零关卡的死信路径。


### concurrency

**范围确认**：读了 target.diff 全部 4 个文件的变更；核对了 `src/walkcode/channel_native_runtime.py` 中 `serve_lark_ws`（2173-2230）、`_process_lark_inbox_item`（2237-2264）、`_report_lark_inbound_failure`（2266-2293）、`_best_effort_flush_outbox`（2654-2662）、`_start/_stop_telegram_maintenance_tasks`、`_compact_state_forever`，以及 `channel_native/__init__.py` 中 `OutboxDispatcher.flush_once`/`_send_claimed_item`（3900-3956）、`DurableOutbox.enqueue` 幂等去重（3585-3613）、`InboundLedger`（3754-3816）。

concurrency 维度逐项证伪结果（均未成立，说明我确实查了而不是没看）：

1. **消费循环改为"任何异常即退出"的取消语义**：`except Exception` 不吞 `CancelledError`（3.8+ 为 BaseException），停机取消时 `.processing` 正确保留、`.failed` 改名被跳过，finally 里 `bridge.stop()` 和维护 task 取消顺序正确。无问题。
2. **通知路径持 `_ingress_lock` 期间调 `_best_effort_flush_outbox`**：flush 有 `wait_for(…, 5.0)` 上界，dispatcher 不回取 `_ingress_lock`（3030 行契约注释与 dispatcher 实现均核实），无锁序反转、无死锁；`_compact_state_forever` 同锁但同步执行，阻塞有界。
3. **flush 单飞**：`flush_once` 有 `_flush_lock`，与维护任务 `_flush_outbox_forever` 并发调用不会同批双发；超时取消在 `async with` 内正常放锁。超时把已 claim 条目留在 60s 租约里，恢复路径是 context.md 第 14 行已确认的既有机制，不重复上报。
4. **重启重放 vs 飞书重推的双通知竞态**：notice 先 `save_state` 再 flush（2292→2293），outbox `enqueue` 按 `lark-inbound-failed:{event_id}` 跨 pending/sent/dead 去重（3592-3594，含重启后场景）；ledger `complete()` 对未 start 的 id 也安全（3783-3785），不会抛 KeyError 打死消费者。
5. **同 handle 双排水**：重放扫描在 `bridge.start()` 之前入队，单消费者且全程在 `_ingress_lock` 内，新事件文件名 time_ns+uuid 唯一，无双排水路径（与 attack_surface 提示一致）。
6. **回调门禁改动（2277 行）**：仅镜像主路径的 sender 门禁语义（主路径 1966-1968 对 callback 本就不查 sender），固定文案通知不执行回调动作，chat 门禁保留；无新增并发面。
7. **取消 mid-send 导致重发**：`wait_for` 取消可能落在 `send_view` 成功与 `record_result` 之间造成重发，但该暴露面在维护 flusher 里早已存在，且本 diff 的 ARCHITECTURE.md 变更明确声明非 exactly-once，属已文档化的接受行为。
8. **确定性恢复失败导致的重启循环**：前一轮已作为 "permanent-freeze claim" 被驳回，context.md 明令不再加重启监督者，不重报。

无达到上报门槛的 finding。

VERDICT: SAFE
SUMMARY: 本次增量在并发维度是收紧而非放松——消费循环失败语义更严格、flush 有界且单飞、通知幂等键跨重启去重、取消路径保留 processing 文件，未发现新增竞态或死锁。


### data

**范围确认**：审查了 target.diff 全部内容（AGENTS.md、ARCHITECTURE.md、docs/lark-profile-deploy.md、src/walkcode/channel_native_runtime.py、tests/test_channel_native_lark.py），并读取了仓库源码中 `_process_lark_inbox_item`、`_report_lark_inbound_failure`、`_best_effort_flush_outbox`、`serve_lark_ws`、`DurableOutbox.enqueue`（幂等去重）、`InboundLedger`、Lark `parse_event` 及 `lark_live.py` 发送寻址逻辑。维度：data（状态/outbox 持久化、幂等、部分失败、恢复安全、留存关系）。

核对结论（data 视角）：R1 兑现（通知保存失败 → 异常穿透 except 块 → `.processing` 留存 → 消费循环 raise 退出）；R2 兑现（enqueue → save_state → 5 秒 `_best_effort_flush_outbox`，超时留存 pending）；通知重放的重复投递由 `enqueue` 的 idempotency_key 跨 pending/sent/dead 去重兜住；`.failed` 留存与启动重放过滤一致。发现一处收件人守卫缺口：

### ISSUE_1
- Plain: 卡片按钮类事件若没有会话和消息编号，失败通知仍会入队，永远发不出去，最后堆成死信
- Severity: Warning
- Category: DataIntegrity
- File: src/walkcode/channel_native_runtime.py
- StartLine: 2274
- EndLine: 2291
- Symbol: _report_lark_inbound_failure
- Confidence: 0.6
- PreExisting: no
- Evidence: 该函数为失败入站事件入队一条固定文案通知。收件人守卫只覆盖 parse_event 抛异常的情况（`lark_inbound_failure_unaddressable`）；解析成功但地址为空的 callback 不拦：`_lark_chat_allowed` 对 callback 且 chat_id 为空直接放行（1836-1838 行注释自述"Card callbacks may omit the chat context"），本次新改的 `if inbound.callback is None and not self._lark_sender_allowed(...)` 又让 callback 跳过 sender 白名单。而 parse_event 的 fallback 分支把一切无 `message` 键的 payload 都解析成 callback（chat_id/message_id 可全空）。lark_live.py 的发送逻辑：root_id 为空且 chat_id 为空时走 `receive_id("")` 的 create 调用，必然失败。
- Problem: 一条 chat_id 与 root/message_id 均为空的 callback 事件处理失败后，通知照常 enqueue + save_state，随后每轮重试都撞 API 错误，退避后进 outbox.dead。改动前，配置了 `LARK_ALLOWED_OPEN_IDS` 的部署（personal 两实例即此形态）里空 sender_id 会被白名单拦下、不产生通知；本次跳过 sender 门后这条不可寻址路径被打开。后果：死信桶被无法投递、也永远无人收到的记录污染，degrade 日志反复刷；同时违反 ARCHITECTURE.md 第 245 行左右的既有不变式"无法识别收件人的畸形 payload 只归档 .failed、不发渠道通知"——目前该不变式只在"解析抛异常"时成立，"解析成功但地址为空"时不成立。
- Fix: 在 `_report_lark_inbound_failure` 解析成功后补一条守卫：`if not inbound.chat_id and not (inbound.root_message_id or inbound.message_id):` 走既有 `_log_degrade("lark_inbound_failure_unaddressable", ...)` 并 return，与 parse 失败分支对齐；补一个空地址 callback 的测试断言 outbox 无新 pending。

VERDICT: NEEDS_FIX
SUMMARY: 可靠投递主链路（留存 .processing、先存后发、幂等去重、超时留存重试）核对无误；唯一缺口是无收件人的 callback 失败通知会入队成必死的死信，与文档承诺的"无收件人不发通知"不一致，需补一条地址守卫。


## 回证与裁决

Verdict: FALSE_POSITIVE
AdjustedSeverity: -
ActualLine: 2275
Quote: `operation.update(kind="reply", message_id=root_id, reply_in_thread=True)` (lark_live.py:93)
Reasoning: 无 chat 的 callback 仍带 message_id（ingress 在 lark_live.py:475 把 context.open_message_id 提升到顶层，Lark card.action.trigger 必有卡片消息上下文），binding 取 `root_message_id or message_id` 后走 reply 寻址不需要 chat_id，通知可投递；ingress 只注册 message_receive 和 card_action 两类事件，"全空地址"payload 不可产出，即使人为构造也只是单条 outbox 项 5 次退避后进 dead 桶（不阻塞队列、原始 payload 仍保留为 .failed），且旧代码 sender 白名单本就不作用于 callback（tests/test_channel_native_lark.py:705 明确为设计行为），issue 声称的回归不成立。


data 的 Warning 已驳回：真实 card.action.trigger 经 ingress 带消息地址，无 chat_id 时仍可 reply。人工构造全空地址的纵深防御建议保留，未为了不可产出的事件扩展本次修复。security 同组 Suggestion 不影响门禁。

## 验证

- 最终全量：1190 tests OK，skipped=9；专项30 tests OK。
- 两个个人飞书应用已分别完成真实 Claude/Codex → 工具卡/回复、空回合兜底、入站失败通知读回、state 保存；测试消息删除。work-claude 真实 E2E 亦通过。
- 保存失败/文件损坏等故障注入通过；真实 hook --defer 0.15 秒完成，不读取故意损坏的 state，队列权限0600。
- 用户确认保持 ack 表情“收到”语义。personal Lark 配额故障通过 bot/message API、state、transcript、dead outbox 完整对齐；用户随后授权迁回个人飞书。部署结果在 lark-profile-deploy.md 更新。

## 留底与边界

修复前快照为 Git HEAD 2ed8ca4，无 stash。第一轮报告含全部十维及五条回证；本轮原始报告保存在上述 RunDir。发送后保存失败仍可重复；.failed 手动处置；不承诺 exactly-once。