# Deep Review 综合结论 — v0.13.1（重投去重前置 / 接管终态卡 / 观察卡元数据回填）

**VERDICT**: NEEDS_FIX →（本轮当场修复 4 条后）**SAFE-WITH-DEFERRED**
**轮次**：1 / 3（--plan-only 出报告；主 agent 择要修复，未启用 skill auto-fix）
**类型**：code

> 范围：PR #58，`main...release/v0.13.1`，被审 commit `4339238`
> Review engine：codex codex-cli 0.144.3（host: claude; engine_source: auto）
> Cursor：composer-2.5 smoke 失败，跳过
> 维度：8 个 codex 并行（correctness/errors/security/concurrency/data/observability/design/tests），全部返回
> Phase 2 验证：9 条已派（另 1 条高共识高自信免验）；结果 8 VERIFIED / 1 FALSE_POSITIVE / 0 UNVERIFIABLE
> Repo: /Users/alpha/workspace/walkcode
> HeadSHA: 4339238cfd5fcc4045aff2414f42e21b86dd4c3f
> RunDir: /var/folders/00/s7tt4dgj53v123y8671yb3b00000gn/T/deep-review-walkcode-4339238-1784010354.qVGY
> 规模：267 行 / 8 文件
> 无 Critical。全部 finding 为 Warning。

## 🔴🔴 顶级必修（本轮已修）

### 1. [Warning] channel_native_runtime.py process_lark_event — 命令分支重投仍重复执行
> **一句话**：本地命令（/status 等）的回复分支没进去重账本，飞书重投同一事件会再答一遍。

- **来源**: correctness 0.92 + errors 0.88 + concurrency 0.90 + design 0.86（4 维度共识）
- **回证**: SKIPPED_HIGH_CONFIDENCE（≥2 维度且 ≥0.9）
- **处置**: ✅ 已修 — 新增 `_complete_lark_local_inbound()`（`ledger.record()`），command / agent-selector / unknown-slash 三个本地终止分支落账；回归测试 `test_redelivered_command_event_is_not_rerun`。

## 🔴 高置信（本轮已修）

### 2. [Warning] 重投丢弃静默，无法观测（observability 0.91，VERIFIED）
✅ 已修 — 早退分支补 `_log_degrade("lark_duplicate_inbound_dropped", event_id/chat_id/message_id/is_callback)`。

### 3. [Warning] 新建观察会话首卡片模型/上下文为空且不再刷新（design 0.82，VERIFIED）
✅ 已修 — 回填 meta 后若有变化立即 `refresh_session_status_card()`，不再等下一个无关事件。

### 4. [Warning] codex rollout 解析缺边界测试（tests 0.86，VERIFIED）
✅ 已修 — 新增 `test_codex_token_count_edge_inputs_are_safe`（info None/{}/坏类型）与 `test_rollout_tail_truncated_line_is_skipped`（>64KB 截断半行）。

## 🟡 遗留（有意不在本版修，均为 Warning）

1. **重复卡片回调等到超时才回兜底 toast**（errors 0.82，VERIFIED）— 回证确认为 main 既有行为（orchestrator 原路径同样先查重后 ack），非本 PR 退化。修法：duplicate 早退前对 callback 做幂等 ack。
2. **transcript stat→open TOCTOU**（security 0.78，VERIFIED）— main 上旧函数即如此，非本 PR 引入。修法：O_NOFOLLOW|O_NONBLOCK 打开后 fstat 校验。本地攻击面（恶意 hook payload），风险低。
3. **首投失败（ledger.fail）后重投会再发一张根卡片，旧卡残留**（data 0.88，VERIFIED）— 这是有意的重试语义副作用，罕见路径；修法需按 event_id 持久化预创建卡并复用，留待后续。
4. **transcript 解析失败全静默**（observability 0.86，VERIFIED）— 需带采样的降级日志，避免每 hook 刷屏，留待后续。
5. **接管终态卡发送失败路径缺测试**（tests 0.88，VERIFIED）— 回证已确认行为安全：该卡走 outbox，send 异常不冒泡、接管仍成功；仅缺用例。

## ❌ 已驳回

- tests#1「fail() 后重投重试无测试」— FALSE_POSITIVE：`test_channel_native_persistence_reliability.py:442` 已覆盖（同 event_id 失败后重投成功，launch_count==2）。

## 维度元信息

| 来源 | VERDICT | issues | exit |
|---|---|---|---|
| dim-correctness | NEEDS_FIX | 1 | 0 |
| dim-errors | NEEDS_FIX | 2 | 0 |
| dim-security | NEEDS_FIX | 1 | 0 |
| dim-concurrency | NEEDS_FIX | 1 | 0 |
| dim-data | NEEDS_FIX | 1 | 0 |
| dim-observability | NEEDS_FIX | 2 | 0 |
| dim-design | NEEDS_FIX | 2 | 0 |
| dim-tests | NEEDS_FIX | 3 | 0 |
| cursor-holistic | (skipped) | — | — |

## 本轮修复后验证

`uv run python -m unittest discover -s tests`：689 tests，OK（含 4 个新回归/边界用例）。

## 原始报告

- 各维度：`$RUN_DIR/dim-{name}.md`；各回证：`$RUN_DIR/verify-{key}.md`；元信息：`$RUN_DIR/meta.txt`
