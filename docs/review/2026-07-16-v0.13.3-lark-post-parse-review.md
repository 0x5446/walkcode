# Deep Review 综合结论 — v0.13.3 飞书 post 图文消息解析修复

**VERDICT**: SAFE（round-2 采纳项修复后）
**轮次**：2 / 3
**类型**：code

> 范围：PR #62（release/v0.13.3 vs main 0fea6da）——修复 msg_type=post 图文混排消息被
> empty_message_ignored 静默丢弃（2026-07-16 sess-1616ac 丢消息事故根本原因）
> Review engine：codex-cli 0.144.4（host: claude; engine_source: auto）
> Cursor：disabled（未登录）
> 维度：8 个 codex 并行（correctness / errors / security / concurrency / data / observability / design / tests）
> Phase 2 验证：以"修复代替回证"处置——全部发现由实现者直接修复或核实源码后书面婉拒，无 Critical
> Repo: /Users/alpha/workspace/walkcode
> HeadSHA: 7001c68（最终）；round-1 @ e2c3f9f，round-2 @ 51fd642
> RunDir: /tmp/deep-review-walkcode-e2c3f9f-*（r1）、/tmp/deep-review-walkcode-51fd642-*（r2）
> 规模：~350 行 / 5 文件（含测试与版本 bump）

## Round 1（e2c3f9f）：4 Warning，全部采纳修复（51fd642）

1. **errors** [W 0.78] 语言层包裹的 post（`{"zh_cn": {...}}`）仍解析为空 → 新增 `_post_bodies` 兼容两种形状 + 回归测试
2. **security** [W 0.82] post 附件数量无上限 → `_MAX_POST_ATTACHMENTS=10` 截断 + `post_attachments_truncated` 降级日志 + 测试
3. **observability** [W 0.86] `_inbound_message_type` 不认 telegram raw 形状 → 按负载键（sticker/voice/...）识别 + 三态单测
4. **tests** [W 0.84] 缺畸形 post、media 下载用例 → 补 `test_parse_post_malformed_segments_are_tolerated`、`test_post_media_attachment_downloads_as_file`

## Round 2（51fd642）：6 Warning，3 采纳（7001c68）3 婉拒

采纳：

1. **data + design 双维度共识** [W 0.92/0.95] 附件截断只有运维日志，agent/用户不知情（部分输入静默丢失）→ `parse_event` 在截断时向正文追加"共 N 个附件，仅保留前 10 个"提示，测试断言 `event.text`
2. **tests-1** [W 0.92] 回归用例未锁事故字段名 `msg_type`（只用了 `message_type`）→ 事故形态 adapter/runtime 用例改用 `msg_type`，`_inbound_message_type` 补 msg_type 别名用例
3. **tests-2** [W 0.86] media 下载缺 `message_id`/`source_message_id` 断言 → 已补

婉拒（记录在案，不阻塞发布）：

1. **security** [W 0.86] 下载无字节上限、`lark_live._download` 全量读内存 —— 存量路径（image/file 消息一直如此），单文件大小受飞书上传限制约束，数量已封顶；如需流式下载应单独立项改 `lark_live`，不属本 PR 范围
2. **concurrency** [W 0.83] 截断前仍完整遍历段落 —— content 已被 `_decode_content` 完整解析进内存，遍历成本相对 JSON decode 可忽略；payload 大小受飞书消息体上限约束
3. **observability** [W 0.86] 畸形段落被 isinstance 守卫跳过时无日志 —— 该守卫是纵深防御，线上从未观测到畸形段落；为纯函数加日志上下文的复杂度收益不成比例

## 验证

- `uv run python -m unittest discover -s tests`：**709 passed**（新增 12 个回归用例）
- 事故复现脚本：post content（含语言层/畸形/超限变体）经 `parse_event` 均产出非空 text/attachments，不再命中 `empty_message_ignored`

## 残留 Issues

无 Critical。婉拒的 3 条 Warning 见上，均为存量行为或收益不成比例的加固，另行立项处理。
