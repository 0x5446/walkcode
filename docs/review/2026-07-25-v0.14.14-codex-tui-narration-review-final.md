# Deep Review 综合结论 — v0.14.14 codex TUI 叙述镜像 + 大响应行

**VERDICT**: NEEDS_FIX → 两轮修复后 **SAFE（带已登记的既有跟进项）**
**轮次**：2 / 3
**类型**：code

> 范围：PR #81 — codex TUI 中段助手文本镜像修复 + codex thread/resume 大响应行修复
> Review engine：codex codex-cli 0.144.5（host: claude; engine_source: auto）
> Cursor：disabled（not logged in）
> 维度：8 个 codex 并行（correctness errors security concurrency data observability design tests，全部成功）；round 2 复审 correctness/data/tests
> Phase 2 验证：6 条已派，全部 VERIFIED；3 组高共识项（≥2 维度且 Confidence ≥ 0.9）按规则免回证直接修复
> Repo: /Users/alpha/workspace/walkcode
> HeadSHA: 550a511f8d4978d6e0d27bd3b1308726c0dd0078（round 1 基线；修复提交 2734c01 / ccaa802）
> RunDir: /var/folders/00/s7tt4dgj53v123y8671yb3b00000gn/T/deep-review-walkcode-550a511-1784981198.Y8vl
> 规模：+323/-15 行 / 7 文件（round 1 基线）

## 已修复（round 1，commit 2734c01）

### 1. [Warning] stop 排水只读一批，超长尾部被 advance 永久跳过
- 来源：correctness + design（高共识，Confidence 0.99）
- 修复：stop 分支循环排水直到游标到达捕获边界或不再前进。

### 2. [Warning] stop 去重按文本值删除全部同文行，误删中段真实消息
- 来源：correctness + data + tests（高共识，Confidence 1.0）
- 修复：只从尾部删除一条与收口气泡相同的行。

### 3. [Warning] 超限 ValueError 后失步进程被复用，后续请求连环失败
- 来源：correctness + errors + concurrency + design（四维共识）
- 修复：`_discard_process` 杀死并清空 `_process`，下次请求全新启动。

### 4. [Warning] takeover 失败先写日志后置失败态，日志抛错会卡住事务
- 来源：errors（单维度）
- 修复：`fail_takeover` 先于 `_log_degrade`。

## 已修复（round 2，commit ccaa802）

### 5. [Warning] `_discard_process` 清空已解析事件缓冲，丢真实会话事件
- 来源：round 2 correctness（Confidence 0.94）
- 修复：保留 `_buffered_notifications`（它们是已发生的完整事件）。

### 6. [Warning] 排水超时的 CancelledError 不被 except Exception 捕获，账本卡 in_progress → 重放被判重复丢弃
- 来源：round 2 data + Phase 2 verify-v5 VERIFIED
- 修复：`process_tui_hook` 改 `except BaseException` 释放账本再重抛。（游标先提交后发送的事务性问题见跟进项 F5）

### 7. [Warning] 测试缺口：绕过真实入口 / 样本低于事故规模 / 缺失败分支与日志断言
- 来源：round 1+2 tests
- 修复：新增入口级 process_tui_hook codex stop 排水测试、真实子进程超限测试、64MiB 上限钉死、1MiB 成功样本、takeover 降级日志断言、必需 hooks 清单字面量断言。

## ❌ 已驳回（Phase 2 / 证据驳回）

- **round 2 data ISSUE_1（`_tui_event_id` 用 turn_id 致同 turn 工具事件互判重复）**：codex 0.144.5 hook payload 携带每事件唯一的 `event_id`（二进制字符串证据），显式 ID 链优先取它；线上单 turn 多张工具卡全部送达。FALSE_POSITIVE。

## 🟡 已验证、登记跟进（均为既有行为，非本 PR 引入；无 Critical）

- **F1（verify-v1 VERIFIED）**：`_read_transcript_narration` 把 OSError 折叠成"游标不变+空"，stop 仍无条件 advance——文件暂时不可读时尾部叙述丢失。
- **F2（verify-v2 VERIFIED）**：排水链路零日志零计数，故障零条与正常零条不可区分（格式漂移会无声丢文本）。
- **F3（verify-v3 VERIFIED）**：`transcript_path` 来自未认证本地 hook，未与 session 绑定校验，存在本机信任边界内的跨会话泄漏面（claude/codex 同构）。
- **F4（verify-v4 VERIFIED）**：stderr 不持续排水 + `_buffered_notifications` 无上限（本地可信 app-server 场景风险低）。
- **F5（verify-v5 VERIFIED）**：游标先提交后发送，取消窗口内已提交未发送的文本丢失——根治需要"先入 outbox 再提交游标"的事务化改造（ADR 级）。
- **F6（verify-v6 VERIFIED）**：叙述游标不落盘，重启后 deferred stop 重放按首见文件快进，尾部叙述无法补发。
- **F7（round 2 correctness ISSUE_1）**：DurableOutbox 对同一 binding 无严格保序，首条瞬时失败重试会落到收口消息之后（影响所有多消息序列，既有行为）。
- **F8（security ISSUE_2，未回证）**：降级日志未做控制字符清理/长度限制/脱敏，日志卫生跟进。

## 维度元信息

| 来源 | VERDICT | issues | exit |
|---|---|---|---|
| dim-correctness | NEEDS_FIX | 3 | 0 |
| dim-errors | NEEDS_FIX | 3 | 0 |
| dim-security | NEEDS_FIX | 3 | 0 |
| dim-concurrency | NEEDS_FIX | 2 | 0 |
| dim-data | NEEDS_FIX | 2 | 0 |
| dim-observability | NEEDS_FIX | 1 | 0 |
| dim-design | NEEDS_FIX | 2 | 0 |
| dim-tests | NEEDS_FIX | 3 | 0 |
| round2 correctness/data/tests | NEEDS_FIX→已处置 | 2/2/3 | 0 |

## 验证

- 全量测试：954 passed, 24 subtests passed（round 2 修复后）
- 原始产物：RunDir 下 dim-*.md / verify-v*.md / dim-*.round2.md
